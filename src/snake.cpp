#include "snake.h"

class Snake
{
  void Snake
  {
    for(int index = 0; index == this->defaultLength; index++)
    {
      this->body.push_back(10, 10, 10);
    }
  };
  ~Snake()
  {
    delete body;
  };
};
